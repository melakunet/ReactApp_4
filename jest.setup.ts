/**
 * jest.setup.ts
 * Runs once before all tests.
 * Imports @testing-library/jest-dom which adds custom DOM matchers to Jest
 * such as: toBeInTheDocument(), toHaveTextContent(), toBeDisabled(), etc.
 */
import '@testing-library/jest-dom';
